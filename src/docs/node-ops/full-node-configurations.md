# 全节点配置

Monad 支持全节点通过 [RaptorCast](/monad-arch/consensus/raptorcast) 协议接收区块提案的多种方式。

## 1. 专用全节点

专用全节点由验证器在其 `[fullnode.identities]` 部分中列入白名单。这些节点通过主 RaptorCast 直接从其上游验证器接收转发的数据块。

**主要特征：**

- 通过主 RaptorCast 从上游验证器接收转发的数据块
- 可以使用 RaptorCast `mode = "None"` 运行，无需参与辅助 RaptorCast
- 仅依赖于上游验证器的可用性
- 占用上游验证器的大量带宽，上游验证器必须将所有接收到的数据块转发给每个下游专用全节点
- 提供最高的可靠性级别

## 2. 通过辅助 RaptorCast

节点进行辅助 RaptorCast，其机制与主 [RaptorCast](/monad-arch/consensus/raptorcast) 相同，不同之处在于每个区块由一个特定的验证器作为发起者，全节点作为其他参与者。

每个辅助 RaptorCast 组根植于单个验证器。

全节点参与辅助 RaptorCast 有两种方式：作为优先全节点或作为公共全节点。

### 2a. 优先全节点

优先全节点由验证器在其 `[fullnode_raptorcast.full_nodes_prioritized.identities]` 部分中列入白名单。这意味着该节点将获得保证加入根植于该验证器的辅助 RaptorCast 组。

**主要特征：**

- 通过辅助 RaptorCast 接收数据块
- 必须使用 RaptorCast `mode = "Client"` 运行以参与辅助 RaptorCast
- 依赖于上游验证器的可用性和组功能
- 提供中等可靠性级别

### 2b. 公共全节点

公共全节点没有被任何验证器明确列入白名单。该节点竞争验证器的辅助 RaptorCast 组中的可用空间。

**主要特征：**

- 通过辅助 RaptorCast 接收数据块（如果是辅助 RaptorCast 组的一部分）
- 必须使用 RaptorCast `mode = "Client"` 运行以参与辅助 RaptorCast
- 依赖于验证器的可用性、组功能和插槽可用性
- 提供最低的可靠性级别

## 配置比较

| 节点类型 | 白名单 | RaptorCast 模式 | 数据源 | 可靠性 | 依赖项 
| **专用** | 是 | `"None"`
（`"Client"` 可选） | 主 RaptorCast | 高 | 仅验证器 
| **优先** | 是 | `"Client"` | 辅助 RaptorCast | 中等 | 验证器 +
RaptorCast 组 
| **公共** | 否 | `"Client"` | 辅助 RaptorCast | 低 | 验证器 +
RaptorCast 组 +
插槽可用性 

## 辅助 RaptorCast 组的形成

辅助 RaptorCast 系统通过组邀请流程运作：

- **组邀请**：当验证器以 `mode = "Publisher"` 运行时，它根据配置的限制向其路由表中的全节点发送组邀请
- **接受/拒绝**：全节点可以根据其当前的组参与情况和配置接受或拒绝邀请
- **组确认**：验证器收集响应并创建持续一段时间的组
- **优先选择**：验证器在每次组形成期间始终向 `full_nodes_prioritized` 中列出的节点发送邀请

## 验证器的 RaptorCast 配置

### `None`

- 禁用辅助 RaptorCast 参与
- 减少带宽使用，因为节点不重新广播数据块

### `Client`

- 启用辅助 RaptorCast 参与
- 由于数据块重新广播而增加带宽使用
- 优先和公共全节点所必需

> **💡 提示**
> 
> 有关 RaptorCast 的更多技术细节，请参阅 [RaptorCast 文档](/monad-arch/consensus/raptorcast)。
