# 归档数据

URL: https://docs.monad.xyz/node-ops/archive-data

## 背景

作为一个高吞吐量且区块频繁的区块链，Monad 生成大量数据 - 包括**交易数据**（区块、交易、收据、日志和跟踪）和**状态数据**（每个区块末尾的完整状态树）。

全节点和验证器在 MonadDB 中尽可能多地存储这些数据，当存储容量达到 80% 时会覆盖最旧的数据。如果 RPC 调用请求的数据比本地可用的数据更旧，节点必须引用外部源。这就是**归档服务器**的作用所在。

## 归档服务器

归档服务器是一个独立的服务器，用于存储历史交易数据。注意，归档服务器不存储状态数据 - 有关原因的讨论请参见[这里](/developer-essentials/historical-data)。

归档服务器的数据由运行额外进程（称为归档写入器）的独立全节点提供。

归档服务器为请求节点上因修剪而不再可用的历史交易数据提供服务。注意，多个全节点和验证器可以指向同一个归档服务器。

## 数据库

### MonadDB (TrieDB)

MonadDB，也称为 TrieDB，是每个全节点和验证器运行的本地状态数据库。它维护最新的状态树以及相应的交易数据。

一旦 MonadDB 的存储容量达到 80%，节点就会开始覆盖最旧的数据。由于这种机制，以及频繁的区块和高链吞吐量，大多数 MonadDB 实例不会存储整个区块链历史。

### ArchiveDB (MongoDB)

ArchiveDB 是运行在归档服务器上的 MongoDB 数据库。它由全节点上的归档写入器进程提供数据。

可以配置全节点和验证器在本地 MonadDB 中数据不可用时使用 ArchiveDB 作为历史数据源。

### 对象存储

作为 MongoDB 的替代方案，归档数据可以存储在对象存储服务中（例如 AWS S3）。

虽然 MongoDB 在性能和查询效率方面更受青睐，但对象存储为偏好异地或基于云的数据保留的用户提供了可行的备用选项。

与 ArchiveDB 一样，对象存储也可以在 RPC 客户端中配置为历史数据源。

## 数据访问顺序

当 RPC 客户端检索数据时，它遵循以下优先级顺序：

1. 链状态缓冲区（内存缓存）
2. MonadDB (TrieDB)
3. ArchiveDB (MongoDB)
4. 对象存储

> **💡 提示**
> 
> 如果同时配置了 ArchiveDB 和对象存储，ArchiveDB 将优先于对象存储。建议同时配置两者，以确保在 ArchiveDB 实例出现任何问题时有备用机制。

## 架构

![Monad 归档服务器架构](/docs-images/monad-archive-server-architecture.png)
## 存储的数据

ArchiveDB 存储以下数据类型：

- 区块
- 交易
- 收据
- 日志
- 跟踪

> **💡 提示**
> 
> 有关历史数据的更多详细信息，请参阅[历史数据](/developer-essentials/historical-data)页面。
